Jacaranda2FA 00.00.31

DNN 10.3.2 popup/mobile field-width correction based on 00.00.30.

- Fixes username, password and 2FA fields remaining full width under the stock DNN login layout.
- Root cause: DNN 10.3.2 default.css applies min-width:100% !important to text/password inputs in dnnFormPopupMobileView.
- CSS min-width overrides max-width, so the 00.00.30 maximum widths could not visually shrink the fields.
- Explicitly resets min-width to 0 !important on Jacaranda2FA credential, authenticator/email-code and recovery-code inputs.
- Retains username/password maximum width of 460px.
- Retains authenticator/email verification code maximum width of 14rem.
- Retains recovery-code maximum width of 18rem.
- Includes exact overrides for DNN's dnnFormPopupMobileView selectors.
- Retains the single-checkbox and checkbox-alignment fixes from 00.00.29/00.00.28.
- No authentication, TOTP, email OTP, recovery-code, trusted-browser security, throttling, policy, cryptographic, or database-schema changes.
- No new SQL migration.
