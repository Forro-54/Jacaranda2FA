Jacaranda2FA 00.00.28

Login-form alignment and accessibility cleanup release based on the confirmed-working 00.00.27 security-hardened baseline.

- Reworks the “Keep me signed in” row so the checkbox is clearly visible and aligned directly beside its label.
- Reworks the “Remember this browser for 2FA” row so the checkbox is clearly visible and aligned directly beside its label.
- Adds explicit checkbox sizing and layout overrides so DNN/theme textbox rules cannot stretch, float or separate these checkbox controls from their text.
- Makes both labels explicitly associated with their checkbox controls, improving click/tap targeting and accessibility.
- Aligns the explanatory help text beneath each checkbox/label pair instead of allowing it to compete with DNN form-label floats.
- Includes a small-screen layout adjustment for the same rows.
- No authentication-flow, password-validation, TOTP, email OTP, recovery-code, trusted-browser security, throttling, database-schema, policy, or cryptographic changes.
- No new SQL migration. The existing 00.00.27 security-hardening database migration remains the current schema level.
